flush ruleset

table inet oroproxy {
  set authenticated_macs {
    type ether_addr
    flags dynamic
  }

  chain prerouting {
    type nat hook prerouting priority dstnat;
    # The AP is setup-only.  HTTP is served directly on port 80, and HTTPS is
    # not intercepted because doing so would break TLS certificate validation.
  }
}
